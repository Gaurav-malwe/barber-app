version: 1
applications:
  - appRoot: frontend
    frontend:
      env:
        NEXT_DISABLE_LIGHTNINGCSS: "1"
      phases:
        preBuild:
          commands:
            - npm ci --include=dev
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
